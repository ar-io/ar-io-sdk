/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { addDecoderSizePrefix, addEncoderSizePrefix, combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU8Decoder, getU8Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type Codec, type Decoder, type Encoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findEscrowPda } from '../pdas/index.js';
import { ARIO_ANT_ESCROW_PROGRAM_ADDRESS } from '../program-address.js';

export const DEPOSIT_ANT_DISCRIMINATOR = new Uint8Array([94, 88, 51, 91, 189, 165, 112, 203]);

export function getDepositAntDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(DEPOSIT_ANT_DISCRIMINATOR); }

export type DepositAntInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountEscrow extends string | AccountMeta<string> = string, TAccountAntAsset extends string | AccountMeta<string> = string, TAccountDepositor extends string | AccountMeta<string> = string, TAccountMplCoreProgram extends string | AccountMeta<string> = "CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, TAccountAntAsset extends string ? WritableAccount<TAccountAntAsset> : TAccountAntAsset, TAccountDepositor extends string ? WritableSignerAccount<TAccountDepositor> & AccountSignerMeta<TAccountDepositor> : TAccountDepositor, TAccountMplCoreProgram extends string ? ReadonlyAccount<TAccountMplCoreProgram> : TAccountMplCoreProgram, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;

export type DepositAntInstructionData = { discriminator: ReadonlyUint8Array; recipientProtocol: number; recipientPubkey: ReadonlyUint8Array;  };

export type DepositAntInstructionDataArgs = { recipientProtocol: number; recipientPubkey: ReadonlyUint8Array;  };

export function getDepositAntInstructionDataEncoder(): Encoder<DepositAntInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['recipientProtocol', getU8Encoder()], ['recipientPubkey', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())]]), (value) => ({ ...value, discriminator: DEPOSIT_ANT_DISCRIMINATOR }));
}

export function getDepositAntInstructionDataDecoder(): Decoder<DepositAntInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['recipientProtocol', getU8Decoder()], ['recipientPubkey', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())]]);
}

export function getDepositAntInstructionDataCodec(): Codec<DepositAntInstructionDataArgs, DepositAntInstructionData> {
    return combineCodec(getDepositAntInstructionDataEncoder(), getDepositAntInstructionDataDecoder());
}

export type DepositAntAsyncInput<TAccountEscrow extends string = string, TAccountAntAsset extends string = string, TAccountDepositor extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> =  {
  /**
 * New escrow PDA, one per ANT mint. `init` enforces "no double
 * deposit" — a second `deposit_ant` for the same mint fails because
 * the PDA already exists.
 */
escrow?: Address<TAccountEscrow>;
/**
 * The Metaplex Core asset (ANT) being escrowed. Constrained to the
 * mpl-core program so callers can't pass a system account or a
 * different program's NFT. Inner-data discriminator is checked in
 * the handler via `read_mpl_core_owner`.
 */
antAsset: Address<TAccountAntAsset>;
/**
 * Caller — must be the current ANT owner. Pays rent + tx fee + signs
 * the mpl-core TransferV1 CPI.
 */
depositor: TransactionSigner<TAccountDepositor>;
/**
 * Metaplex Core program. Pinned to MPL_CORE_PROGRAM_ID so the CPI
 * can't be redirected.
 */
mplCoreProgram?: Address<TAccountMplCoreProgram>;
systemProgram?: Address<TAccountSystemProgram>;
recipientProtocol: DepositAntInstructionDataArgs["recipientProtocol"];
recipientPubkey: DepositAntInstructionDataArgs["recipientPubkey"];
}

export async function getDepositAntInstructionAsync<TAccountEscrow extends string, TAccountAntAsset extends string, TAccountDepositor extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS>(input: DepositAntAsyncInput<TAccountEscrow, TAccountAntAsset, TAccountDepositor, TAccountMplCoreProgram, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): Promise<DepositAntInstruction<TProgramAddress, TAccountEscrow, TAccountAntAsset, TAccountDepositor, TAccountMplCoreProgram, TAccountSystemProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_ANT_ESCROW_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { escrow: { value: input.escrow ?? null, isWritable: true }, antAsset: { value: input.antAsset ?? null, isWritable: true }, depositor: { value: input.depositor ?? null, isWritable: true }, mplCoreProgram: { value: input.mplCoreProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.escrow.value) {
accounts.escrow.value = await findEscrowPda({ antAsset: getAddressFromResolvedInstructionAccount("antAsset", accounts.antAsset.value) }, { programAddress });
}
if (!accounts.mplCoreProgram.value) {
accounts.mplCoreProgram.value = 'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d' as Address<'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("escrow", accounts.escrow), getAccountMeta("antAsset", accounts.antAsset), getAccountMeta("depositor", accounts.depositor), getAccountMeta("mplCoreProgram", accounts.mplCoreProgram), getAccountMeta("systemProgram", accounts.systemProgram)], data: getDepositAntInstructionDataEncoder().encode(args as DepositAntInstructionDataArgs), programAddress } as DepositAntInstruction<TProgramAddress, TAccountEscrow, TAccountAntAsset, TAccountDepositor, TAccountMplCoreProgram, TAccountSystemProgram>);
}

export type DepositAntInput<TAccountEscrow extends string = string, TAccountAntAsset extends string = string, TAccountDepositor extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> =  {
  /**
 * New escrow PDA, one per ANT mint. `init` enforces "no double
 * deposit" — a second `deposit_ant` for the same mint fails because
 * the PDA already exists.
 */
escrow: Address<TAccountEscrow>;
/**
 * The Metaplex Core asset (ANT) being escrowed. Constrained to the
 * mpl-core program so callers can't pass a system account or a
 * different program's NFT. Inner-data discriminator is checked in
 * the handler via `read_mpl_core_owner`.
 */
antAsset: Address<TAccountAntAsset>;
/**
 * Caller — must be the current ANT owner. Pays rent + tx fee + signs
 * the mpl-core TransferV1 CPI.
 */
depositor: TransactionSigner<TAccountDepositor>;
/**
 * Metaplex Core program. Pinned to MPL_CORE_PROGRAM_ID so the CPI
 * can't be redirected.
 */
mplCoreProgram?: Address<TAccountMplCoreProgram>;
systemProgram?: Address<TAccountSystemProgram>;
recipientProtocol: DepositAntInstructionDataArgs["recipientProtocol"];
recipientPubkey: DepositAntInstructionDataArgs["recipientPubkey"];
}

export function getDepositAntInstruction<TAccountEscrow extends string, TAccountAntAsset extends string, TAccountDepositor extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS>(input: DepositAntInput<TAccountEscrow, TAccountAntAsset, TAccountDepositor, TAccountMplCoreProgram, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): DepositAntInstruction<TProgramAddress, TAccountEscrow, TAccountAntAsset, TAccountDepositor, TAccountMplCoreProgram, TAccountSystemProgram> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_ANT_ESCROW_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { escrow: { value: input.escrow ?? null, isWritable: true }, antAsset: { value: input.antAsset ?? null, isWritable: true }, depositor: { value: input.depositor ?? null, isWritable: true }, mplCoreProgram: { value: input.mplCoreProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.mplCoreProgram.value) {
accounts.mplCoreProgram.value = 'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d' as Address<'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("escrow", accounts.escrow), getAccountMeta("antAsset", accounts.antAsset), getAccountMeta("depositor", accounts.depositor), getAccountMeta("mplCoreProgram", accounts.mplCoreProgram), getAccountMeta("systemProgram", accounts.systemProgram)], data: getDepositAntInstructionDataEncoder().encode(args as DepositAntInstructionDataArgs), programAddress } as DepositAntInstruction<TProgramAddress, TAccountEscrow, TAccountAntAsset, TAccountDepositor, TAccountMplCoreProgram, TAccountSystemProgram>);
}

export type ParsedDepositAntInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/**
 * New escrow PDA, one per ANT mint. `init` enforces "no double
 * deposit" — a second `deposit_ant` for the same mint fails because
 * the PDA already exists.
 */
escrow: TAccountMetas[0];
/**
 * The Metaplex Core asset (ANT) being escrowed. Constrained to the
 * mpl-core program so callers can't pass a system account or a
 * different program's NFT. Inner-data discriminator is checked in
 * the handler via `read_mpl_core_owner`.
 */
antAsset: TAccountMetas[1];
/**
 * Caller — must be the current ANT owner. Pays rent + tx fee + signs
 * the mpl-core TransferV1 CPI.
 */
depositor: TAccountMetas[2];
/**
 * Metaplex Core program. Pinned to MPL_CORE_PROGRAM_ID so the CPI
 * can't be redirected.
 */
mplCoreProgram: TAccountMetas[3];
systemProgram: TAccountMetas[4];
};
data: DepositAntInstructionData; };

export function parseDepositAntInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedDepositAntInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 5 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { escrow: getNextAccount(), antAsset: getNextAccount(), depositor: getNextAccount(), mplCoreProgram: getNextAccount(), systemProgram: getNextAccount() }, data: getDepositAntInstructionDataDecoder().decode(instruction.data) };
}