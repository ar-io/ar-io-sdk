/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { ARIO_ANT_ESCROW_PROGRAM_ADDRESS } from '../program-address.js';

export const CLAIM_ANT_ARWEAVE_DISCRIMINATOR = new Uint8Array([103, 22, 240, 40, 162, 212, 3, 108]);

export function getClaimAntArweaveDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_ANT_ARWEAVE_DISCRIMINATOR); }

export type ClaimAntArweaveInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountEscrow extends string | AccountMeta<string> = string, TAccountAntAsset extends string | AccountMeta<string> = string, TAccountClaimant extends string | AccountMeta<string> = string, TAccountDepositor extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountMplCoreProgram extends string | AccountMeta<string> = "CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, TAccountAntAsset extends string ? WritableAccount<TAccountAntAsset> : TAccountAntAsset, TAccountClaimant extends string ? ReadonlyAccount<TAccountClaimant> : TAccountClaimant, TAccountDepositor extends string ? WritableAccount<TAccountDepositor> : TAccountDepositor, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountMplCoreProgram extends string ? ReadonlyAccount<TAccountMplCoreProgram> : TAccountMplCoreProgram, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;

export type ClaimAntArweaveInstructionData = { discriminator: ReadonlyUint8Array; messageNonce: ReadonlyUint8Array; signature: ReadonlyUint8Array; saltLen: number;  };

export type ClaimAntArweaveInstructionDataArgs = { messageNonce: ReadonlyUint8Array; signature: ReadonlyUint8Array; saltLen: number;  };

export function getClaimAntArweaveInstructionDataEncoder(): FixedSizeEncoder<ClaimAntArweaveInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['messageNonce', fixEncoderSize(getBytesEncoder(), 32)], ['signature', fixEncoderSize(getBytesEncoder(), 512)], ['saltLen', getU8Encoder()]]), (value) => ({ ...value, discriminator: CLAIM_ANT_ARWEAVE_DISCRIMINATOR }));
}

export function getClaimAntArweaveInstructionDataDecoder(): FixedSizeDecoder<ClaimAntArweaveInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['messageNonce', fixDecoderSize(getBytesDecoder(), 32)], ['signature', fixDecoderSize(getBytesDecoder(), 512)], ['saltLen', getU8Decoder()]]);
}

export function getClaimAntArweaveInstructionDataCodec(): FixedSizeCodec<ClaimAntArweaveInstructionDataArgs, ClaimAntArweaveInstructionData> {
    return combineCodec(getClaimAntArweaveInstructionDataEncoder(), getClaimAntArweaveInstructionDataDecoder());
}

export type ClaimAntArweaveInput<TAccountEscrow extends string = string, TAccountAntAsset extends string = string, TAccountClaimant extends string = string, TAccountDepositor extends string = string, TAccountPayer extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> =  {
  /**
 * Escrow PDA. `has_one = depositor` ties the close-rent recipient to
 * the original depositor account passed below; `close = depositor`
 * returns rent on successful claim. Cached `bump` is re-derived via
 * the seeds constraint for defense-in-depth.
 */
escrow: Address<TAccountEscrow>;
/**
 * The Metaplex Core asset being released. Must match
 * `escrow.ant_mint` so a misrouted account can't redirect the
 * transfer.
 */
antAsset: Address<TAccountAntAsset>;
/**
 * Recipient of the ANT — its pubkey is bound into the canonical
 * message that the Arweave key signed. Front-runners can resubmit
 * the tx but cannot redirect the asset.
 * no on-chain ownership check needed (anyone can be a claimant).
 */
claimant: Address<TAccountClaimant>;
/**
 * Original depositor — receives rent on escrow close. Pinned via
 * `has_one = depositor` on the escrow above.
 */
depositor: Address<TAccountDepositor>;
/**
 * Tx fee payer. Doesn't have to be the claimant — anyone can submit
 * a valid claim signature on the recipient's behalf.
 */
payer: TransactionSigner<TAccountPayer>;
mplCoreProgram?: Address<TAccountMplCoreProgram>;
systemProgram?: Address<TAccountSystemProgram>;
messageNonce: ClaimAntArweaveInstructionDataArgs["messageNonce"];
signature: ClaimAntArweaveInstructionDataArgs["signature"];
saltLen: ClaimAntArweaveInstructionDataArgs["saltLen"];
}

export function getClaimAntArweaveInstruction<TAccountEscrow extends string, TAccountAntAsset extends string, TAccountClaimant extends string, TAccountDepositor extends string, TAccountPayer extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS>(input: ClaimAntArweaveInput<TAccountEscrow, TAccountAntAsset, TAccountClaimant, TAccountDepositor, TAccountPayer, TAccountMplCoreProgram, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): ClaimAntArweaveInstruction<TProgramAddress, TAccountEscrow, TAccountAntAsset, TAccountClaimant, TAccountDepositor, TAccountPayer, TAccountMplCoreProgram, TAccountSystemProgram> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_ANT_ESCROW_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { escrow: { value: input.escrow ?? null, isWritable: true }, antAsset: { value: input.antAsset ?? null, isWritable: true }, claimant: { value: input.claimant ?? null, isWritable: false }, depositor: { value: input.depositor ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, mplCoreProgram: { value: input.mplCoreProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.mplCoreProgram.value) {
accounts.mplCoreProgram.value = 'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d' as Address<'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("escrow", accounts.escrow), getAccountMeta("antAsset", accounts.antAsset), getAccountMeta("claimant", accounts.claimant), getAccountMeta("depositor", accounts.depositor), getAccountMeta("payer", accounts.payer), getAccountMeta("mplCoreProgram", accounts.mplCoreProgram), getAccountMeta("systemProgram", accounts.systemProgram)], data: getClaimAntArweaveInstructionDataEncoder().encode(args as ClaimAntArweaveInstructionDataArgs), programAddress } as ClaimAntArweaveInstruction<TProgramAddress, TAccountEscrow, TAccountAntAsset, TAccountClaimant, TAccountDepositor, TAccountPayer, TAccountMplCoreProgram, TAccountSystemProgram>);
}

export type ParsedClaimAntArweaveInstruction<TProgram extends string = typeof ARIO_ANT_ESCROW_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/**
 * Escrow PDA. `has_one = depositor` ties the close-rent recipient to
 * the original depositor account passed below; `close = depositor`
 * returns rent on successful claim. Cached `bump` is re-derived via
 * the seeds constraint for defense-in-depth.
 */
escrow: TAccountMetas[0];
/**
 * The Metaplex Core asset being released. Must match
 * `escrow.ant_mint` so a misrouted account can't redirect the
 * transfer.
 */
antAsset: TAccountMetas[1];
/**
 * Recipient of the ANT — its pubkey is bound into the canonical
 * message that the Arweave key signed. Front-runners can resubmit
 * the tx but cannot redirect the asset.
 * no on-chain ownership check needed (anyone can be a claimant).
 */
claimant: TAccountMetas[2];
/**
 * Original depositor — receives rent on escrow close. Pinned via
 * `has_one = depositor` on the escrow above.
 */
depositor: TAccountMetas[3];
/**
 * Tx fee payer. Doesn't have to be the claimant — anyone can submit
 * a valid claim signature on the recipient's behalf.
 */
payer: TAccountMetas[4];
mplCoreProgram: TAccountMetas[5];
systemProgram: TAccountMetas[6];
};
data: ClaimAntArweaveInstructionData; };

export function parseClaimAntArweaveInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedClaimAntArweaveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 7 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { escrow: getNextAccount(), antAsset: getNextAccount(), claimant: getNextAccount(), depositor: getNextAccount(), payer: getNextAccount(), mplCoreProgram: getNextAccount(), systemProgram: getNextAccount() }, data: getClaimAntArweaveInstructionDataDecoder().decode(instruction.data) };
}