/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { getGatewaySlotDecoder, getGatewaySlotEncoder, type GatewaySlot, type GatewaySlotArgs } from '../types/index.js';

export const GATEWAY_REGISTRY_DISCRIMINATOR = new Uint8Array([207, 115, 197, 33, 28, 106, 182, 209]);

export function getGatewayRegistryDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(GATEWAY_REGISTRY_DISCRIMINATOR); }

export type GatewayRegistry = { discriminator: ReadonlyUint8Array; 
/** L-8: Vestigial field — not checked at runtime. Kept to preserve account layout. */
authority: Address; count: number; padding: ReadonlyUint8Array; 
/**
 * DEVNET-SIZED on the `deploy/devnet` branch: 30 slots instead of the
 * production 3_000. This is a smoke-test devnet — full AO snapshot
 * import (664 gateways) is intentionally out-of-scope; that flow is
 * exercised on localnet. **Do not merge this change to `main`** —
 * mainnet must keep the full 3_000 capacity. Bumped from 10→30 to give
 * downstream-app developers headroom to `join-network` (10 imported
 * fixtures + 20 dev seats); see `resize_gateway_registry` for the
 * admin instruction that grows the existing on-chain account in place.
 */
gateways: Array<GatewaySlot>;  };

export type GatewayRegistryArgs = { 
/** L-8: Vestigial field — not checked at runtime. Kept to preserve account layout. */
authority: Address; count: number; padding: ReadonlyUint8Array; 
/**
 * DEVNET-SIZED on the `deploy/devnet` branch: 30 slots instead of the
 * production 3_000. This is a smoke-test devnet — full AO snapshot
 * import (664 gateways) is intentionally out-of-scope; that flow is
 * exercised on localnet. **Do not merge this change to `main`** —
 * mainnet must keep the full 3_000 capacity. Bumped from 10→30 to give
 * downstream-app developers headroom to `join-network` (10 imported
 * fixtures + 20 dev seats); see `resize_gateway_registry` for the
 * admin instruction that grows the existing on-chain account in place.
 */
gateways: Array<GatewaySlotArgs>;  };

/** Gets the encoder for {@link GatewayRegistryArgs} account data. */
export function getGatewayRegistryEncoder(): FixedSizeEncoder<GatewayRegistryArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['authority', getAddressEncoder()], ['count', getU32Encoder()], ['padding', fixEncoderSize(getBytesEncoder(), 4)], ['gateways', getArrayEncoder(getGatewaySlotEncoder(), { size: 30 })]]), (value) => ({ ...value, discriminator: GATEWAY_REGISTRY_DISCRIMINATOR }));
}

/** Gets the decoder for {@link GatewayRegistry} account data. */
export function getGatewayRegistryDecoder(): FixedSizeDecoder<GatewayRegistry> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['authority', getAddressDecoder()], ['count', getU32Decoder()], ['padding', fixDecoderSize(getBytesDecoder(), 4)], ['gateways', getArrayDecoder(getGatewaySlotDecoder(), { size: 30 })]]);
}

/** Gets the codec for {@link GatewayRegistry} account data. */
export function getGatewayRegistryCodec(): FixedSizeCodec<GatewayRegistryArgs, GatewayRegistry> {
    return combineCodec(getGatewayRegistryEncoder(), getGatewayRegistryDecoder());
}

export function decodeGatewayRegistry<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<GatewayRegistry, TAddress>;
export function decodeGatewayRegistry<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<GatewayRegistry, TAddress>;
export function decodeGatewayRegistry<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<GatewayRegistry, TAddress> | MaybeAccount<GatewayRegistry, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getGatewayRegistryDecoder());
}

export async function fetchGatewayRegistry<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<GatewayRegistry, TAddress>> {
  const maybeAccount = await fetchMaybeGatewayRegistry(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGatewayRegistry<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<GatewayRegistry, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGatewayRegistry(maybeAccount);
}

export async function fetchAllGatewayRegistry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<GatewayRegistry>[]> {
  const maybeAccounts = await fetchAllMaybeGatewayRegistry(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGatewayRegistry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<GatewayRegistry>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGatewayRegistry(maybeAccount));
}

export function getGatewayRegistrySize(): number {
  return 1728;
}