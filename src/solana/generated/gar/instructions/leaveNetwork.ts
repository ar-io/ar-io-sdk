/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findDecreaseOperatorStakeWithdrawalCounterPda, findEpochSettingsPda, findGatewayPda, findRegistryPda, findSettingsPda } from '../pdas/index.js';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../program-address.js';

export const LEAVE_NETWORK_DISCRIMINATOR = new Uint8Array([95, 180, 37, 177, 111, 193, 20, 19]);

export function getLeaveNetworkDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(LEAVE_NETWORK_DISCRIMINATOR); }

export type LeaveNetworkInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountRegistry extends string | AccountMeta<string> = string, TAccountSettings extends string | AccountMeta<string> = string, TAccountGateway extends string | AccountMeta<string> = string, TAccountEpochSettings extends string | AccountMeta<string> = string, TAccountWithdrawalCounter extends string | AccountMeta<string> = string, TAccountWithdrawal extends string | AccountMeta<string> = string, TAccountExcessWithdrawal extends string | AccountMeta<string> = string, TAccountOperator extends string | AccountMeta<string> = string, TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountRegistry extends string ? WritableAccount<TAccountRegistry> : TAccountRegistry, TAccountSettings extends string ? WritableAccount<TAccountSettings> : TAccountSettings, TAccountGateway extends string ? WritableAccount<TAccountGateway> : TAccountGateway, TAccountEpochSettings extends string ? ReadonlyAccount<TAccountEpochSettings> : TAccountEpochSettings, TAccountWithdrawalCounter extends string ? WritableAccount<TAccountWithdrawalCounter> : TAccountWithdrawalCounter, TAccountWithdrawal extends string ? WritableAccount<TAccountWithdrawal> : TAccountWithdrawal, TAccountExcessWithdrawal extends string ? WritableAccount<TAccountExcessWithdrawal> : TAccountExcessWithdrawal, TAccountOperator extends string ? WritableSignerAccount<TAccountOperator> & AccountSignerMeta<TAccountOperator> : TAccountOperator, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;

export type LeaveNetworkInstructionData = { discriminator: ReadonlyUint8Array;  };

export type LeaveNetworkInstructionDataArgs = {  };

export function getLeaveNetworkInstructionDataEncoder(): FixedSizeEncoder<LeaveNetworkInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: LEAVE_NETWORK_DISCRIMINATOR }));
}

export function getLeaveNetworkInstructionDataDecoder(): FixedSizeDecoder<LeaveNetworkInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getLeaveNetworkInstructionDataCodec(): FixedSizeCodec<LeaveNetworkInstructionDataArgs, LeaveNetworkInstructionData> {
    return combineCodec(getLeaveNetworkInstructionDataEncoder(), getLeaveNetworkInstructionDataDecoder());
}

export type LeaveNetworkAsyncInput<TAccountRegistry extends string = string, TAccountSettings extends string = string, TAccountGateway extends string = string, TAccountEpochSettings extends string = string, TAccountWithdrawalCounter extends string = string, TAccountWithdrawal extends string = string, TAccountExcessWithdrawal extends string = string, TAccountOperator extends string = string, TAccountSystemProgram extends string = string> =  {
  registry?: Address<TAccountRegistry>;
settings?: Address<TAccountSettings>;
gateway?: Address<TAccountGateway>;
/**
 * Read-only — only consulted to snapshot `epoch_duration` onto the
 * gateway so `finalize_gone` can compute the GC eligibility window
 * from a value frozen at leave time (defense against admin retuning).
 */
epochSettings?: Address<TAccountEpochSettings>;
withdrawalCounter?: Address<TAccountWithdrawalCounter>;
/**
 * Anchor-init slot: holds the **protected exit vault** (`is_protected:
 * true`) for `min(min_operator_stake, operator_stake)` — Lua's
 * `createGatewayExitVault`. Always created when there's any stake to
 * vault; an immediately-claimable zero-amount placeholder is kept for
 * the rent-reclaim path (matches the prune L-6 behavior, generalized
 * to leave for symmetry).
 */
withdrawal: Address<TAccountWithdrawal>;
/**
 * Optional **excess vault** slot (`is_protected: false`) — Lua's
 * `createGatewayWithdrawVault` for the `operator_stake - min_stake`
 * portion. The handler creates it manually (system_program::create_account
 * CPI) when `excess_amount > 0`; passes through unmodified when zero.
 * Caller derives PDA off-chain using `WITHDRAWAL_COUNTER.next_id + 1`
 * (i.e. the slot AFTER the exit vault).
 *
 * via system_program CPI, and the data write all run only when the
 * excess amount is non-zero. Skipped slot stays unowned.
 */
excessWithdrawal?: Address<TAccountExcessWithdrawal>;
operator: TransactionSigner<TAccountOperator>;
systemProgram?: Address<TAccountSystemProgram>;
}

export async function getLeaveNetworkInstructionAsync<TAccountRegistry extends string, TAccountSettings extends string, TAccountGateway extends string, TAccountEpochSettings extends string, TAccountWithdrawalCounter extends string, TAccountWithdrawal extends string, TAccountExcessWithdrawal extends string, TAccountOperator extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: LeaveNetworkAsyncInput<TAccountRegistry, TAccountSettings, TAccountGateway, TAccountEpochSettings, TAccountWithdrawalCounter, TAccountWithdrawal, TAccountExcessWithdrawal, TAccountOperator, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): Promise<LeaveNetworkInstruction<TProgramAddress, TAccountRegistry, TAccountSettings, TAccountGateway, TAccountEpochSettings, TAccountWithdrawalCounter, TAccountWithdrawal, TAccountExcessWithdrawal, TAccountOperator, TAccountSystemProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { registry: { value: input.registry ?? null, isWritable: true }, settings: { value: input.settings ?? null, isWritable: true }, gateway: { value: input.gateway ?? null, isWritable: true }, epochSettings: { value: input.epochSettings ?? null, isWritable: false }, withdrawalCounter: { value: input.withdrawalCounter ?? null, isWritable: true }, withdrawal: { value: input.withdrawal ?? null, isWritable: true }, excessWithdrawal: { value: input.excessWithdrawal ?? null, isWritable: true }, operator: { value: input.operator ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Resolve default values.
if (!accounts.registry.value) {
accounts.registry.value = await findRegistryPda();
}
if (!accounts.settings.value) {
accounts.settings.value = await findSettingsPda();
}
if (!accounts.gateway.value) {
accounts.gateway.value = await findGatewayPda({ operator: getAddressFromResolvedInstructionAccount("operator", accounts.operator.value) }, { programAddress });
}
if (!accounts.epochSettings.value) {
accounts.epochSettings.value = await findEpochSettingsPda();
}
if (!accounts.withdrawalCounter.value) {
accounts.withdrawalCounter.value = await findDecreaseOperatorStakeWithdrawalCounterPda({ operator: getAddressFromResolvedInstructionAccount("operator", accounts.operator.value) }, { programAddress });
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("registry", accounts.registry), getAccountMeta("settings", accounts.settings), getAccountMeta("gateway", accounts.gateway), getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("withdrawalCounter", accounts.withdrawalCounter), getAccountMeta("withdrawal", accounts.withdrawal), getAccountMeta("excessWithdrawal", accounts.excessWithdrawal), getAccountMeta("operator", accounts.operator), getAccountMeta("systemProgram", accounts.systemProgram)], data: getLeaveNetworkInstructionDataEncoder().encode({}), programAddress } as LeaveNetworkInstruction<TProgramAddress, TAccountRegistry, TAccountSettings, TAccountGateway, TAccountEpochSettings, TAccountWithdrawalCounter, TAccountWithdrawal, TAccountExcessWithdrawal, TAccountOperator, TAccountSystemProgram>);
}

export type LeaveNetworkInput<TAccountRegistry extends string = string, TAccountSettings extends string = string, TAccountGateway extends string = string, TAccountEpochSettings extends string = string, TAccountWithdrawalCounter extends string = string, TAccountWithdrawal extends string = string, TAccountExcessWithdrawal extends string = string, TAccountOperator extends string = string, TAccountSystemProgram extends string = string> =  {
  registry: Address<TAccountRegistry>;
settings: Address<TAccountSettings>;
gateway: Address<TAccountGateway>;
/**
 * Read-only — only consulted to snapshot `epoch_duration` onto the
 * gateway so `finalize_gone` can compute the GC eligibility window
 * from a value frozen at leave time (defense against admin retuning).
 */
epochSettings: Address<TAccountEpochSettings>;
withdrawalCounter: Address<TAccountWithdrawalCounter>;
/**
 * Anchor-init slot: holds the **protected exit vault** (`is_protected:
 * true`) for `min(min_operator_stake, operator_stake)` — Lua's
 * `createGatewayExitVault`. Always created when there's any stake to
 * vault; an immediately-claimable zero-amount placeholder is kept for
 * the rent-reclaim path (matches the prune L-6 behavior, generalized
 * to leave for symmetry).
 */
withdrawal: Address<TAccountWithdrawal>;
/**
 * Optional **excess vault** slot (`is_protected: false`) — Lua's
 * `createGatewayWithdrawVault` for the `operator_stake - min_stake`
 * portion. The handler creates it manually (system_program::create_account
 * CPI) when `excess_amount > 0`; passes through unmodified when zero.
 * Caller derives PDA off-chain using `WITHDRAWAL_COUNTER.next_id + 1`
 * (i.e. the slot AFTER the exit vault).
 *
 * via system_program CPI, and the data write all run only when the
 * excess amount is non-zero. Skipped slot stays unowned.
 */
excessWithdrawal?: Address<TAccountExcessWithdrawal>;
operator: TransactionSigner<TAccountOperator>;
systemProgram?: Address<TAccountSystemProgram>;
}

export function getLeaveNetworkInstruction<TAccountRegistry extends string, TAccountSettings extends string, TAccountGateway extends string, TAccountEpochSettings extends string, TAccountWithdrawalCounter extends string, TAccountWithdrawal extends string, TAccountExcessWithdrawal extends string, TAccountOperator extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: LeaveNetworkInput<TAccountRegistry, TAccountSettings, TAccountGateway, TAccountEpochSettings, TAccountWithdrawalCounter, TAccountWithdrawal, TAccountExcessWithdrawal, TAccountOperator, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): LeaveNetworkInstruction<TProgramAddress, TAccountRegistry, TAccountSettings, TAccountGateway, TAccountEpochSettings, TAccountWithdrawalCounter, TAccountWithdrawal, TAccountExcessWithdrawal, TAccountOperator, TAccountSystemProgram> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { registry: { value: input.registry ?? null, isWritable: true }, settings: { value: input.settings ?? null, isWritable: true }, gateway: { value: input.gateway ?? null, isWritable: true }, epochSettings: { value: input.epochSettings ?? null, isWritable: false }, withdrawalCounter: { value: input.withdrawalCounter ?? null, isWritable: true }, withdrawal: { value: input.withdrawal ?? null, isWritable: true }, excessWithdrawal: { value: input.excessWithdrawal ?? null, isWritable: true }, operator: { value: input.operator ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Resolve default values.
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("registry", accounts.registry), getAccountMeta("settings", accounts.settings), getAccountMeta("gateway", accounts.gateway), getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("withdrawalCounter", accounts.withdrawalCounter), getAccountMeta("withdrawal", accounts.withdrawal), getAccountMeta("excessWithdrawal", accounts.excessWithdrawal), getAccountMeta("operator", accounts.operator), getAccountMeta("systemProgram", accounts.systemProgram)], data: getLeaveNetworkInstructionDataEncoder().encode({}), programAddress } as LeaveNetworkInstruction<TProgramAddress, TAccountRegistry, TAccountSettings, TAccountGateway, TAccountEpochSettings, TAccountWithdrawalCounter, TAccountWithdrawal, TAccountExcessWithdrawal, TAccountOperator, TAccountSystemProgram>);
}

export type ParsedLeaveNetworkInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
registry: TAccountMetas[0];
settings: TAccountMetas[1];
gateway: TAccountMetas[2];
/**
 * Read-only — only consulted to snapshot `epoch_duration` onto the
 * gateway so `finalize_gone` can compute the GC eligibility window
 * from a value frozen at leave time (defense against admin retuning).
 */
epochSettings: TAccountMetas[3];
withdrawalCounter: TAccountMetas[4];
/**
 * Anchor-init slot: holds the **protected exit vault** (`is_protected:
 * true`) for `min(min_operator_stake, operator_stake)` — Lua's
 * `createGatewayExitVault`. Always created when there's any stake to
 * vault; an immediately-claimable zero-amount placeholder is kept for
 * the rent-reclaim path (matches the prune L-6 behavior, generalized
 * to leave for symmetry).
 */
withdrawal: TAccountMetas[5];
/**
 * Optional **excess vault** slot (`is_protected: false`) — Lua's
 * `createGatewayWithdrawVault` for the `operator_stake - min_stake`
 * portion. The handler creates it manually (system_program::create_account
 * CPI) when `excess_amount > 0`; passes through unmodified when zero.
 * Caller derives PDA off-chain using `WITHDRAWAL_COUNTER.next_id + 1`
 * (i.e. the slot AFTER the exit vault).
 *
 * via system_program CPI, and the data write all run only when the
 * excess amount is non-zero. Skipped slot stays unowned.
 */
excessWithdrawal?: TAccountMetas[6] | undefined;
operator: TAccountMetas[7];
systemProgram: TAccountMetas[8];
};
data: LeaveNetworkInstructionData; };

export function parseLeaveNetworkInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedLeaveNetworkInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 9 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
const getNextOptionalAccount = () => {
  const accountMeta = getNextAccount();
  return accountMeta.address === ARIO_GAR_PROGRAM_ADDRESS ? undefined : accountMeta;
};
  return { programAddress: instruction.programAddress, accounts: { registry: getNextAccount(), settings: getNextAccount(), gateway: getNextAccount(), epochSettings: getNextAccount(), withdrawalCounter: getNextAccount(), withdrawal: getNextAccount(), excessWithdrawal: getNextOptionalAccount(), operator: getNextAccount(), systemProgram: getNextAccount() }, data: getLeaveNetworkInstructionDataDecoder().decode(instruction.data) };
}