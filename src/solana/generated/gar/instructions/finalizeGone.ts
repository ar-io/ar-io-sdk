/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findEpochSettingsPda, findRegistryPda } from '../pdas/index.js';
import { ARIO_GAR_PROGRAM_ADDRESS } from '../program-address.js';

export const FINALIZE_GONE_DISCRIMINATOR = new Uint8Array([119, 118, 254, 204, 248, 64, 134, 228]);

export function getFinalizeGoneDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(FINALIZE_GONE_DISCRIMINATOR); }

export type FinalizeGoneInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountGateway extends string | AccountMeta<string> = string, TAccountRegistry extends string | AccountMeta<string> = string, TAccountEpochSettings extends string | AccountMeta<string> = string, TAccountCaller extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountGateway extends string ? WritableAccount<TAccountGateway> : TAccountGateway, TAccountRegistry extends string ? WritableAccount<TAccountRegistry> : TAccountRegistry, TAccountEpochSettings extends string ? ReadonlyAccount<TAccountEpochSettings> : TAccountEpochSettings, TAccountCaller extends string ? WritableSignerAccount<TAccountCaller> & AccountSignerMeta<TAccountCaller> : TAccountCaller, ...TRemainingAccounts]>;

export type FinalizeGoneInstructionData = { discriminator: ReadonlyUint8Array;  };

export type FinalizeGoneInstructionDataArgs = {  };

export function getFinalizeGoneInstructionDataEncoder(): FixedSizeEncoder<FinalizeGoneInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: FINALIZE_GONE_DISCRIMINATOR }));
}

export function getFinalizeGoneInstructionDataDecoder(): FixedSizeDecoder<FinalizeGoneInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getFinalizeGoneInstructionDataCodec(): FixedSizeCodec<FinalizeGoneInstructionDataArgs, FinalizeGoneInstructionData> {
    return combineCodec(getFinalizeGoneInstructionDataEncoder(), getFinalizeGoneInstructionDataDecoder());
}

export type FinalizeGoneAsyncInput<TAccountGateway extends string = string, TAccountRegistry extends string = string, TAccountEpochSettings extends string = string, TAccountCaller extends string = string> =  {
  gateway: Address<TAccountGateway>;
registry?: Address<TAccountRegistry>;
/**
 * Read-only — used to compute the GC eligibility window
 * against `max(gateway.leave_epoch_duration, epoch_settings.epoch_duration)`.
 */
epochSettings?: Address<TAccountEpochSettings>;
/**
 * Permissionless: anyone willing to pay the tx fee can crank GC and
 * receives the Gateway PDA's rent as the cleanup incentive.
 */
caller: TransactionSigner<TAccountCaller>;
}

export async function getFinalizeGoneInstructionAsync<TAccountGateway extends string, TAccountRegistry extends string, TAccountEpochSettings extends string, TAccountCaller extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: FinalizeGoneAsyncInput<TAccountGateway, TAccountRegistry, TAccountEpochSettings, TAccountCaller>, config?: { programAddress?: TProgramAddress } ): Promise<FinalizeGoneInstruction<TProgramAddress, TAccountGateway, TAccountRegistry, TAccountEpochSettings, TAccountCaller>> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { gateway: { value: input.gateway ?? null, isWritable: true }, registry: { value: input.registry ?? null, isWritable: true }, epochSettings: { value: input.epochSettings ?? null, isWritable: false }, caller: { value: input.caller ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Resolve default values.
if (!accounts.registry.value) {
accounts.registry.value = await findRegistryPda();
}
if (!accounts.epochSettings.value) {
accounts.epochSettings.value = await findEpochSettingsPda();
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("gateway", accounts.gateway), getAccountMeta("registry", accounts.registry), getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("caller", accounts.caller)], data: getFinalizeGoneInstructionDataEncoder().encode({}), programAddress } as FinalizeGoneInstruction<TProgramAddress, TAccountGateway, TAccountRegistry, TAccountEpochSettings, TAccountCaller>);
}

export type FinalizeGoneInput<TAccountGateway extends string = string, TAccountRegistry extends string = string, TAccountEpochSettings extends string = string, TAccountCaller extends string = string> =  {
  gateway: Address<TAccountGateway>;
registry: Address<TAccountRegistry>;
/**
 * Read-only — used to compute the GC eligibility window
 * against `max(gateway.leave_epoch_duration, epoch_settings.epoch_duration)`.
 */
epochSettings: Address<TAccountEpochSettings>;
/**
 * Permissionless: anyone willing to pay the tx fee can crank GC and
 * receives the Gateway PDA's rent as the cleanup incentive.
 */
caller: TransactionSigner<TAccountCaller>;
}

export function getFinalizeGoneInstruction<TAccountGateway extends string, TAccountRegistry extends string, TAccountEpochSettings extends string, TAccountCaller extends string, TProgramAddress extends Address = typeof ARIO_GAR_PROGRAM_ADDRESS>(input: FinalizeGoneInput<TAccountGateway, TAccountRegistry, TAccountEpochSettings, TAccountCaller>, config?: { programAddress?: TProgramAddress } ): FinalizeGoneInstruction<TProgramAddress, TAccountGateway, TAccountRegistry, TAccountEpochSettings, TAccountCaller> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_GAR_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { gateway: { value: input.gateway ?? null, isWritable: true }, registry: { value: input.registry ?? null, isWritable: true }, epochSettings: { value: input.epochSettings ?? null, isWritable: false }, caller: { value: input.caller ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("gateway", accounts.gateway), getAccountMeta("registry", accounts.registry), getAccountMeta("epochSettings", accounts.epochSettings), getAccountMeta("caller", accounts.caller)], data: getFinalizeGoneInstructionDataEncoder().encode({}), programAddress } as FinalizeGoneInstruction<TProgramAddress, TAccountGateway, TAccountRegistry, TAccountEpochSettings, TAccountCaller>);
}

export type ParsedFinalizeGoneInstruction<TProgram extends string = typeof ARIO_GAR_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
gateway: TAccountMetas[0];
registry: TAccountMetas[1];
/**
 * Read-only — used to compute the GC eligibility window
 * against `max(gateway.leave_epoch_duration, epoch_settings.epoch_duration)`.
 */
epochSettings: TAccountMetas[2];
/**
 * Permissionless: anyone willing to pay the tx fee can crank GC and
 * receives the Gateway PDA's rent as the cleanup incentive.
 */
caller: TAccountMetas[3];
};
data: FinalizeGoneInstructionData; };

export function parseFinalizeGoneInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedFinalizeGoneInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 4 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { gateway: getNextAccount(), registry: getNextAccount(), epochSettings: getNextAccount(), caller: getNextAccount() }, data: getFinalizeGoneInstructionDataDecoder().decode(instruction.data) };
}