/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { addDecoderSizePrefix, addEncoderSizePrefix, combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getUtf8Decoder, getUtf8Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type Codec, type Decoder, type Encoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { ARIO_ANT_PROGRAM_ADDRESS } from '../program-address.js';

export const SYNC_ATTRIBUTES_DISCRIMINATOR = new Uint8Array([202, 89, 217, 75, 83, 91, 40, 34]);

export function getSyncAttributesDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(SYNC_ATTRIBUTES_DISCRIMINATOR); }

export type SyncAttributesInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountAsset extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TAccountArnsRecord extends string | AccountMeta<string> = string, TAccountMplCoreProgram extends string | AccountMeta<string> = "CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountArnsRecord extends string ? ReadonlyAccount<TAccountArnsRecord> : TAccountArnsRecord, TAccountMplCoreProgram extends string ? ReadonlyAccount<TAccountMplCoreProgram> : TAccountMplCoreProgram, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;

export type SyncAttributesInstructionData = { discriminator: ReadonlyUint8Array; name: string;  };

export type SyncAttributesInstructionDataArgs = { name: string;  };

export function getSyncAttributesInstructionDataEncoder(): Encoder<SyncAttributesInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())]]), (value) => ({ ...value, discriminator: SYNC_ATTRIBUTES_DISCRIMINATOR }));
}

export function getSyncAttributesInstructionDataDecoder(): Decoder<SyncAttributesInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())]]);
}

export function getSyncAttributesInstructionDataCodec(): Codec<SyncAttributesInstructionDataArgs, SyncAttributesInstructionData> {
    return combineCodec(getSyncAttributesInstructionDataEncoder(), getSyncAttributesInstructionDataDecoder());
}

export type SyncAttributesInput<TAccountAsset extends string = string, TAccountPayer extends string = string, TAccountAuthority extends string = string, TAccountArnsRecord extends string = string, TAccountMplCoreProgram extends string = string, TAccountSystemProgram extends string = string> =  {
  /** The Metaplex Core asset whose Attributes plugin will be updated. */
asset: Address<TAccountAsset>;
/**
 * Pays for any rent the inner CPI may charge. Often the same key as
 * `authority` (ANT owner) but kept separate so a sponsor can pay.
 */
payer: TransactionSigner<TAccountPayer>;
/**
 * Must equal the asset's current MPL Core owner — the Attributes
 * plugin uses `Owner` as its `BasePluginAuthority`, so only the
 * holder can sign UpdatePluginV1.
 */
authority: TransactionSigner<TAccountAuthority>;
/** PDA seeds + `ant == asset.key()` are all validated in the handler. */
arnsRecord: Address<TAccountArnsRecord>;
/**
 * Metaplex Core program — pinned to the canonical ID; required for
 * the inner CPI.
 */
mplCoreProgram?: Address<TAccountMplCoreProgram>;
systemProgram?: Address<TAccountSystemProgram>;
name: SyncAttributesInstructionDataArgs["name"];
}

export function getSyncAttributesInstruction<TAccountAsset extends string, TAccountPayer extends string, TAccountAuthority extends string, TAccountArnsRecord extends string, TAccountMplCoreProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_ANT_PROGRAM_ADDRESS>(input: SyncAttributesInput<TAccountAsset, TAccountPayer, TAccountAuthority, TAccountArnsRecord, TAccountMplCoreProgram, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): SyncAttributesInstruction<TProgramAddress, TAccountAsset, TAccountPayer, TAccountAuthority, TAccountArnsRecord, TAccountMplCoreProgram, TAccountSystemProgram> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_ANT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { asset: { value: input.asset ?? null, isWritable: true }, payer: { value: input.payer ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: false }, arnsRecord: { value: input.arnsRecord ?? null, isWritable: false }, mplCoreProgram: { value: input.mplCoreProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.mplCoreProgram.value) {
accounts.mplCoreProgram.value = 'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d' as Address<'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("asset", accounts.asset), getAccountMeta("payer", accounts.payer), getAccountMeta("authority", accounts.authority), getAccountMeta("arnsRecord", accounts.arnsRecord), getAccountMeta("mplCoreProgram", accounts.mplCoreProgram), getAccountMeta("systemProgram", accounts.systemProgram)], data: getSyncAttributesInstructionDataEncoder().encode(args as SyncAttributesInstructionDataArgs), programAddress } as SyncAttributesInstruction<TProgramAddress, TAccountAsset, TAccountPayer, TAccountAuthority, TAccountArnsRecord, TAccountMplCoreProgram, TAccountSystemProgram>);
}

export type ParsedSyncAttributesInstruction<TProgram extends string = typeof ARIO_ANT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/** The Metaplex Core asset whose Attributes plugin will be updated. */
asset: TAccountMetas[0];
/**
 * Pays for any rent the inner CPI may charge. Often the same key as
 * `authority` (ANT owner) but kept separate so a sponsor can pay.
 */
payer: TAccountMetas[1];
/**
 * Must equal the asset's current MPL Core owner — the Attributes
 * plugin uses `Owner` as its `BasePluginAuthority`, so only the
 * holder can sign UpdatePluginV1.
 */
authority: TAccountMetas[2];
/** PDA seeds + `ant == asset.key()` are all validated in the handler. */
arnsRecord: TAccountMetas[3];
/**
 * Metaplex Core program — pinned to the canonical ID; required for
 * the inner CPI.
 */
mplCoreProgram: TAccountMetas[4];
systemProgram: TAccountMetas[5];
};
data: SyncAttributesInstructionData; };

export function parseSyncAttributesInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedSyncAttributesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 6 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { asset: getNextAccount(), payer: getNextAccount(), authority: getNextAccount(), arnsRecord: getNextAccount(), mplCoreProgram: getNextAccount(), systemProgram: getNextAccount() }, data: getSyncAttributesInstructionDataDecoder().decode(instruction.data) };
}