/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, getNonNullResolvedInstructionInput, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findBalancePda, findConfigPda } from '../pdas/index.js';
import { ARIO_CORE_PROGRAM_ADDRESS } from '../program-address.js';

export const IMPORT_BALANCE_DISCRIMINATOR = new Uint8Array([110, 212, 27, 144, 95, 174, 85, 171]);

export function getImportBalanceDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(IMPORT_BALANCE_DISCRIMINATOR); }

export type ImportBalanceInstruction<TProgram extends string = typeof ARIO_CORE_PROGRAM_ADDRESS, TAccountConfig extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TAccountPayer extends string | AccountMeta<string> = string, TAccountBalance extends string | AccountMeta<string> = string, TAccountProtocolTokenAccount extends string | AccountMeta<string> = string, TAccountRecipientTokenAccount extends string | AccountMeta<string> = string, TAccountRecipientOwner extends string | AccountMeta<string> = string, TAccountArioMint extends string | AccountMeta<string> = string, TAccountTokenProgram extends string | AccountMeta<string> = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA", TAccountAssociatedTokenProgram extends string | AccountMeta<string> = "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountConfig extends string ? ReadonlyAccount<TAccountConfig> : TAccountConfig, TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer> : TAccountPayer, TAccountBalance extends string ? WritableAccount<TAccountBalance> : TAccountBalance, TAccountProtocolTokenAccount extends string ? WritableAccount<TAccountProtocolTokenAccount> : TAccountProtocolTokenAccount, TAccountRecipientTokenAccount extends string ? WritableAccount<TAccountRecipientTokenAccount> : TAccountRecipientTokenAccount, TAccountRecipientOwner extends string ? ReadonlyAccount<TAccountRecipientOwner> : TAccountRecipientOwner, TAccountArioMint extends string ? ReadonlyAccount<TAccountArioMint> : TAccountArioMint, TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram, TAccountAssociatedTokenProgram extends string ? ReadonlyAccount<TAccountAssociatedTokenProgram> : TAccountAssociatedTokenProgram, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;

export type ImportBalanceInstructionData = { discriminator: ReadonlyUint8Array; owner: Address; amount: bigint;  };

export type ImportBalanceInstructionDataArgs = { owner: Address; amount: number | bigint;  };

export function getImportBalanceInstructionDataEncoder(): FixedSizeEncoder<ImportBalanceInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['owner', getAddressEncoder()], ['amount', getU64Encoder()]]), (value) => ({ ...value, discriminator: IMPORT_BALANCE_DISCRIMINATOR }));
}

export function getImportBalanceInstructionDataDecoder(): FixedSizeDecoder<ImportBalanceInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['owner', getAddressDecoder()], ['amount', getU64Decoder()]]);
}

export function getImportBalanceInstructionDataCodec(): FixedSizeCodec<ImportBalanceInstructionDataArgs, ImportBalanceInstructionData> {
    return combineCodec(getImportBalanceInstructionDataEncoder(), getImportBalanceInstructionDataDecoder());
}

export type ImportBalanceAsyncInput<TAccountConfig extends string = string, TAccountAuthority extends string = string, TAccountPayer extends string = string, TAccountBalance extends string = string, TAccountProtocolTokenAccount extends string = string, TAccountRecipientTokenAccount extends string = string, TAccountRecipientOwner extends string = string, TAccountArioMint extends string = string, TAccountTokenProgram extends string = string, TAccountAssociatedTokenProgram extends string = string, TAccountSystemProgram extends string = string> =  {
  config?: Address<TAccountConfig>;
authority: TransactionSigner<TAccountAuthority>;
payer: TransactionSigner<TAccountPayer>;
/**
 * `init` (not `init_if_needed`) so a re-import for the same owner is
 * rejected with `account already in use`. Solana txs are atomic, so
 * there's no partial-success state to recover from — if this PDA
 * exists, the import already ran and (for pre-registered owners)
 * already moved SPL tokens. Failing loud beats silent double-transfer
 * when the orchestrator's progress tracker is wrong.
 */
balance?: Address<TAccountBalance>;
/**
 * Treasury source for the SPL transfer. Must be `config.treasury`
 * (the protocol_token_account on the ARIO mint). Owned (SPL-level)
 * by the ArioConfig PDA — see `release_treasury_authority` for the
 * one-shot ownership migration on networks where the treasury was
 * originally created under a different authority.
 */
protocolTokenAccount: Address<TAccountProtocolTokenAccount>;
/**
 * Recipient ATA for the owner. `init_if_needed` so callers don't
 * have to pre-create — the migration orchestrator can fan out
 * imports without a per-holder ATA-creation pre-pass.
 */
recipientTokenAccount?: Address<TAccountRecipientTokenAccount>;
/**
 * `associated_token::authority` constraint above and by the `owner`
 * instruction-arg constraint here (they must match — the import is
 * for `owner`'s share, not some third party's).
 */
recipientOwner: Address<TAccountRecipientOwner>;
arioMint: Address<TAccountArioMint>;
tokenProgram?: Address<TAccountTokenProgram>;
associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
systemProgram?: Address<TAccountSystemProgram>;
owner: ImportBalanceInstructionDataArgs["owner"];
amount: ImportBalanceInstructionDataArgs["amount"];
}

export async function getImportBalanceInstructionAsync<TAccountConfig extends string, TAccountAuthority extends string, TAccountPayer extends string, TAccountBalance extends string, TAccountProtocolTokenAccount extends string, TAccountRecipientTokenAccount extends string, TAccountRecipientOwner extends string, TAccountArioMint extends string, TAccountTokenProgram extends string, TAccountAssociatedTokenProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_CORE_PROGRAM_ADDRESS>(input: ImportBalanceAsyncInput<TAccountConfig, TAccountAuthority, TAccountPayer, TAccountBalance, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountRecipientOwner, TAccountArioMint, TAccountTokenProgram, TAccountAssociatedTokenProgram, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): Promise<ImportBalanceInstruction<TProgramAddress, TAccountConfig, TAccountAuthority, TAccountPayer, TAccountBalance, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountRecipientOwner, TAccountArioMint, TAccountTokenProgram, TAccountAssociatedTokenProgram, TAccountSystemProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_CORE_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { config: { value: input.config ?? null, isWritable: false }, authority: { value: input.authority ?? null, isWritable: false }, payer: { value: input.payer ?? null, isWritable: true }, balance: { value: input.balance ?? null, isWritable: true }, protocolTokenAccount: { value: input.protocolTokenAccount ?? null, isWritable: true }, recipientTokenAccount: { value: input.recipientTokenAccount ?? null, isWritable: true }, recipientOwner: { value: input.recipientOwner ?? null, isWritable: false }, arioMint: { value: input.arioMint ?? null, isWritable: false }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false }, associatedTokenProgram: { value: input.associatedTokenProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.config.value) {
accounts.config.value = await findConfigPda();
}
if (!accounts.balance.value) {
accounts.balance.value = await findBalancePda({ owner: getNonNullResolvedInstructionInput("owner", args.owner) }, { programAddress });
}
if (!accounts.recipientTokenAccount.value) {
accounts.recipientTokenAccount.value = await getProgramDerivedAddress({ programAddress: 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL' as Address<'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'>, seeds: [getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("recipientOwner", accounts.recipientOwner.value)), getBytesEncoder().encode(new Uint8Array([6, 221, 246, 225, 215, 101, 161, 147, 217, 203, 225, 70, 206, 235, 121, 172, 28, 180, 133, 237, 95, 91, 55, 145, 58, 140, 245, 133, 126, 255, 0, 169])), getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("arioMint", accounts.arioMint.value))] });
}
if (!accounts.tokenProgram.value) {
accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
}
if (!accounts.associatedTokenProgram.value) {
accounts.associatedTokenProgram.value = 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL' as Address<'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("config", accounts.config), getAccountMeta("authority", accounts.authority), getAccountMeta("payer", accounts.payer), getAccountMeta("balance", accounts.balance), getAccountMeta("protocolTokenAccount", accounts.protocolTokenAccount), getAccountMeta("recipientTokenAccount", accounts.recipientTokenAccount), getAccountMeta("recipientOwner", accounts.recipientOwner), getAccountMeta("arioMint", accounts.arioMint), getAccountMeta("tokenProgram", accounts.tokenProgram), getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram), getAccountMeta("systemProgram", accounts.systemProgram)], data: getImportBalanceInstructionDataEncoder().encode(args as ImportBalanceInstructionDataArgs), programAddress } as ImportBalanceInstruction<TProgramAddress, TAccountConfig, TAccountAuthority, TAccountPayer, TAccountBalance, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountRecipientOwner, TAccountArioMint, TAccountTokenProgram, TAccountAssociatedTokenProgram, TAccountSystemProgram>);
}

export type ImportBalanceInput<TAccountConfig extends string = string, TAccountAuthority extends string = string, TAccountPayer extends string = string, TAccountBalance extends string = string, TAccountProtocolTokenAccount extends string = string, TAccountRecipientTokenAccount extends string = string, TAccountRecipientOwner extends string = string, TAccountArioMint extends string = string, TAccountTokenProgram extends string = string, TAccountAssociatedTokenProgram extends string = string, TAccountSystemProgram extends string = string> =  {
  config: Address<TAccountConfig>;
authority: TransactionSigner<TAccountAuthority>;
payer: TransactionSigner<TAccountPayer>;
/**
 * `init` (not `init_if_needed`) so a re-import for the same owner is
 * rejected with `account already in use`. Solana txs are atomic, so
 * there's no partial-success state to recover from — if this PDA
 * exists, the import already ran and (for pre-registered owners)
 * already moved SPL tokens. Failing loud beats silent double-transfer
 * when the orchestrator's progress tracker is wrong.
 */
balance: Address<TAccountBalance>;
/**
 * Treasury source for the SPL transfer. Must be `config.treasury`
 * (the protocol_token_account on the ARIO mint). Owned (SPL-level)
 * by the ArioConfig PDA — see `release_treasury_authority` for the
 * one-shot ownership migration on networks where the treasury was
 * originally created under a different authority.
 */
protocolTokenAccount: Address<TAccountProtocolTokenAccount>;
/**
 * Recipient ATA for the owner. `init_if_needed` so callers don't
 * have to pre-create — the migration orchestrator can fan out
 * imports without a per-holder ATA-creation pre-pass.
 */
recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
/**
 * `associated_token::authority` constraint above and by the `owner`
 * instruction-arg constraint here (they must match — the import is
 * for `owner`'s share, not some third party's).
 */
recipientOwner: Address<TAccountRecipientOwner>;
arioMint: Address<TAccountArioMint>;
tokenProgram?: Address<TAccountTokenProgram>;
associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
systemProgram?: Address<TAccountSystemProgram>;
owner: ImportBalanceInstructionDataArgs["owner"];
amount: ImportBalanceInstructionDataArgs["amount"];
}

export function getImportBalanceInstruction<TAccountConfig extends string, TAccountAuthority extends string, TAccountPayer extends string, TAccountBalance extends string, TAccountProtocolTokenAccount extends string, TAccountRecipientTokenAccount extends string, TAccountRecipientOwner extends string, TAccountArioMint extends string, TAccountTokenProgram extends string, TAccountAssociatedTokenProgram extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof ARIO_CORE_PROGRAM_ADDRESS>(input: ImportBalanceInput<TAccountConfig, TAccountAuthority, TAccountPayer, TAccountBalance, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountRecipientOwner, TAccountArioMint, TAccountTokenProgram, TAccountAssociatedTokenProgram, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): ImportBalanceInstruction<TProgramAddress, TAccountConfig, TAccountAuthority, TAccountPayer, TAccountBalance, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountRecipientOwner, TAccountArioMint, TAccountTokenProgram, TAccountAssociatedTokenProgram, TAccountSystemProgram> {
  // Program address.
const programAddress = config?.programAddress ?? ARIO_CORE_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { config: { value: input.config ?? null, isWritable: false }, authority: { value: input.authority ?? null, isWritable: false }, payer: { value: input.payer ?? null, isWritable: true }, balance: { value: input.balance ?? null, isWritable: true }, protocolTokenAccount: { value: input.protocolTokenAccount ?? null, isWritable: true }, recipientTokenAccount: { value: input.recipientTokenAccount ?? null, isWritable: true }, recipientOwner: { value: input.recipientOwner ?? null, isWritable: false }, arioMint: { value: input.arioMint ?? null, isWritable: false }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false }, associatedTokenProgram: { value: input.associatedTokenProgram ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.tokenProgram.value) {
accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
}
if (!accounts.associatedTokenProgram.value) {
accounts.associatedTokenProgram.value = 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL' as Address<'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("config", accounts.config), getAccountMeta("authority", accounts.authority), getAccountMeta("payer", accounts.payer), getAccountMeta("balance", accounts.balance), getAccountMeta("protocolTokenAccount", accounts.protocolTokenAccount), getAccountMeta("recipientTokenAccount", accounts.recipientTokenAccount), getAccountMeta("recipientOwner", accounts.recipientOwner), getAccountMeta("arioMint", accounts.arioMint), getAccountMeta("tokenProgram", accounts.tokenProgram), getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram), getAccountMeta("systemProgram", accounts.systemProgram)], data: getImportBalanceInstructionDataEncoder().encode(args as ImportBalanceInstructionDataArgs), programAddress } as ImportBalanceInstruction<TProgramAddress, TAccountConfig, TAccountAuthority, TAccountPayer, TAccountBalance, TAccountProtocolTokenAccount, TAccountRecipientTokenAccount, TAccountRecipientOwner, TAccountArioMint, TAccountTokenProgram, TAccountAssociatedTokenProgram, TAccountSystemProgram>);
}

export type ParsedImportBalanceInstruction<TProgram extends string = typeof ARIO_CORE_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
config: TAccountMetas[0];
authority: TAccountMetas[1];
payer: TAccountMetas[2];
/**
 * `init` (not `init_if_needed`) so a re-import for the same owner is
 * rejected with `account already in use`. Solana txs are atomic, so
 * there's no partial-success state to recover from — if this PDA
 * exists, the import already ran and (for pre-registered owners)
 * already moved SPL tokens. Failing loud beats silent double-transfer
 * when the orchestrator's progress tracker is wrong.
 */
balance: TAccountMetas[3];
/**
 * Treasury source for the SPL transfer. Must be `config.treasury`
 * (the protocol_token_account on the ARIO mint). Owned (SPL-level)
 * by the ArioConfig PDA — see `release_treasury_authority` for the
 * one-shot ownership migration on networks where the treasury was
 * originally created under a different authority.
 */
protocolTokenAccount: TAccountMetas[4];
/**
 * Recipient ATA for the owner. `init_if_needed` so callers don't
 * have to pre-create — the migration orchestrator can fan out
 * imports without a per-holder ATA-creation pre-pass.
 */
recipientTokenAccount: TAccountMetas[5];
/**
 * `associated_token::authority` constraint above and by the `owner`
 * instruction-arg constraint here (they must match — the import is
 * for `owner`'s share, not some third party's).
 */
recipientOwner: TAccountMetas[6];
arioMint: TAccountMetas[7];
tokenProgram: TAccountMetas[8];
associatedTokenProgram: TAccountMetas[9];
systemProgram: TAccountMetas[10];
};
data: ImportBalanceInstructionData; };

export function parseImportBalanceInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedImportBalanceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 11 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { config: getNextAccount(), authority: getNextAccount(), payer: getNextAccount(), balance: getNextAccount(), protocolTokenAccount: getNextAccount(), recipientTokenAccount: getNextAccount(), recipientOwner: getNextAccount(), arioMint: getNextAccount(), tokenProgram: getNextAccount(), associatedTokenProgram: getNextAccount(), systemProgram: getNextAccount() }, data: getImportBalanceInstructionDataDecoder().decode(instruction.data) };
}